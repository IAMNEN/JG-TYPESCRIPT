var person = {
    name: "Ravi",
    scores: [95, 88, 76],
};
var fullname = person.name, _a = person.scores, math = _a[0], science = _a[1], english = _a[2];
console.log(fullname);
console.log(math);
